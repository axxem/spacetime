services:
  mosquitto:
    image: eclipse-mosquitto:1.6.7
    container_name: mqtt
    restart: unless-stopped
    ports:
      - "1883:1883"
      - "8883:8883"
    volumes:
      - ./config:/mosquitto/config/mosquitto.config
      - ./data:/mosquitto/data
      - ./log:/mosquitto/log