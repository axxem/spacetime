services:
  count:
    image: ${REGISTRY:-spacetimehub}/count:${VERSION:-latest}
    container_name: count
    restart: unless-stopped
    ports:
      - "8097:80"
    networks:
      - traefik_default

networks:
  traefik_default:
    external: true